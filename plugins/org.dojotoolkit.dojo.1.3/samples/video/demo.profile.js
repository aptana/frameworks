dependencies = {
	layers: [
		{
			name: "../../src.js",
			resourceName: "video.src",
			dependencies: [
				"video.src"
			]
		}
	],

	prefixes: [
		[ "dijit", "../dijit" ],
		[ "dojox", "../dojox" ],
		[ "demos", "../demos" ]
	]
}
